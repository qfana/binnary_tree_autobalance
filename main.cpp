#include <iostream>

// Вариант C: Обход с использованием ссылок на родительские узлы (если они реализованы).
// Конструктор из файла с матрицей смежности (для неориентированного графа, где строки/столбцы - вершины, а значения - наличие ребра).

int main() {
    

    return 0;
}